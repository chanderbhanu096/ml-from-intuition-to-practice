# Module 08 · Unsupervised learning

> Find structure without labels, and resist believing it is real.

**0 of 8 chapters complete** · ~7 learner-hours · Core

---

## What this module is for

Unsupervised learning: structure without labels. The defining property is that there is no score, so every check has to come from outside the algorithm.

The module spends as much time on *how to be sceptical about a clustering* as on the algorithms themselves, because a clustering that is wrong produces exactly the same confident output as one that is right.

## Before you start

- **Needs:** 02-06, 03-07
- **Unlocks:** 11-06, 12-06, 14-03
- **If you are short of time:** **08-02** (distance and scaling) and **08-06** (stability) matter more than any individual algorithm.

## Algorithms and methods introduced here

| Chapter | What you will be able to run |
|---|---|
| 08-03 | `KMeans` |
| 08-04 | `AgglomerativeClustering`, dendrograms |
| 08-05 | `DBSCAN`, `GaussianMixture` |
| 08-07 | `PCA` |
| 08-08 | `TSNE`, UMAP (discussed), `IsolationForest`, `LocalOutlierFactor` |

## Chapters

| | Chapter | Level | Time | The one idea |
|---|---|---|---|---|
| `[ ]` | **08-01** Learning without labels: what it can and cannot establish | Core | 45m | Structure without labels means no score, so every check comes from outside the algorithm. |
| `[ ]` | **08-02** Similarity, distance, scaling, high-dimensional strangeness | Core | 55m | Distance is decided by your scaling and your columns, long before the algorithm runs. |
| `[ ]` | **08-03** k-means | Core | 60m | k-means finds the k you asked for, in any data, always. |
| `[ ]` | **08-04** Hierarchical clustering | Core | 50m | A dendrogram postpones the choice of k and does not remove it. |
| `[ ]` | **08-05** DBSCAN and Gaussian mixtures | Core | 60m | Density and mixtures: clusters that are not blobs, and points that belong to none. |
| `[ ]` | **08-06** Evaluating clusters, stability, responsible interpretation | Core | 55m | Stability under resampling is the closest thing to validation clustering has. |
| `[ ]` | **08-07** PCA | Core | 60m | PCA rotates to the directions of most variance - which is not the same as most meaning. |
| `[ ]` | **08-08** t-SNE and UMAP limits; anomaly detection; association rules | Core | 60m | Neighbour embeddings distort distance by design, so never measure on a t-SNE plot. |

## Data used

- datasets shipped with scikit-learn
- generated data with a known truth

Synthetic data is always labelled **SYNTHETIC** in the notebook, and is used where knowing the
true answer in advance is the only way to check whether a method finds it.

## Where things are

- Learner notebooks: this folder
- Worked solutions: [`../../solutions/08_unsupervised/`](../../solutions/08_unsupervised/)
- Full curriculum with prerequisites: [`../../CURRICULUM.md`](../../CURRICULUM.md)
- Terms introduced so far: [`../../GLOSSARY.md`](../../GLOSSARY.md)

---

*This file is generated by `scripts/build_module_readmes.py` from `curriculum.yml`. Edit the prose in `scripts/module_content.py` and re-run.*
