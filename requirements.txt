# Core course environment. Python 3.11 recommended (3.10-3.12 fine).
# Install:  python3.11 -m venv .venv && .venv/bin/pip install -r requirements.txt
numpy>=1.26,<3
pandas>=2.1
matplotlib>=3.8
scikit-learn>=1.4
jupyter>=1.0
nbconvert>=7.10
nbformat>=5.9
ipykernel>=6.29

# Authoring only. The animated figures in assets/ are built by
# scripts/build_animations.py, which needs Pillow - already a matplotlib
# dependency, so there is nothing extra for a learner to install. Learners
# only ever view the committed .gif and .svg files.

# Added later, only when the course reaches the module that needs it.
# Module 09 (time series):        statsmodels>=0.14
# Module 10 (neural networks):    torch>=2.2   (CPU build is enough)
# Module 05/11 (boosting, extras): scikit-learn's HistGradientBoosting is used
#                                  instead of xgboost/lightgbm - no extra install.
